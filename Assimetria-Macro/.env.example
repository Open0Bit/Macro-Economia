# =============================================================================
# ARQUIVO DE CONFIGURAÇÃO - Framework de Preparação Assimétrica
# =============================================================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env: cp .env.example .env
# 2. Ajuste os valores conforme necessário
# 3. NÃO commite o arquivo .env (já está no .gitignore)
#
# =============================================================================

# -----------------------------------------------------------------------------
# API KEYS (Opcional)
# -----------------------------------------------------------------------------

# FRED (Federal Reserve Economic Data)
# Obtenha sua chave gratuita em: https://fred.stlouisfed.org/docs/api/api_key.html
# Se não fornecer, o script usará proxies alternativos
FRED_API_KEY=

# Alpha Vantage (Opcional - para dados alternativos)
# https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_KEY=

# -----------------------------------------------------------------------------
# PARÂMETROS DE ANÁLISE
# -----------------------------------------------------------------------------

# Período de análise (formato: YYYY-MM-DD)
START_DATE=2020-01-01
END_DATE=2024-12-31

# ETFs para análise (separados por vírgula)
# Padrão: China ETFs + Defensivos
ETFS=FXI,MCHI,KWEB,GLD,SPY,TLT

# ETFs adicionais para análise customizada (opcional)
# Exemplo para Brasil: CUSTOM_ETFS=EWZ,BRXX
CUSTOM_ETFS=

# -----------------------------------------------------------------------------
# PARÂMETROS ESTATÍSTICOS
# -----------------------------------------------------------------------------

# Número máximo de lags para análise VAR
VAR_MAX_LAG=6

# Critério de seleção de lag (aic, bic, hqic)
VAR_IC_CRITERION=aic

# Janela para correlação rolling (dias)
ROLLING_WINDOW=90

# Threshold para eventos de estresse (múltiplo do desvio padrão)
STRESS_THRESHOLD=2.0

# Nível de significância para testes estatísticos
SIGNIFICANCE_LEVEL=0.05

# -----------------------------------------------------------------------------
# PARÂMETROS DE PROCESSAMENTO
# -----------------------------------------------------------------------------

# Frequência de agregação para análise temporal (D=diário, W=semanal, M=mensal)
AGGREGATION_FREQ=M

# Método de preenchimento de dados faltantes (ffill, bfill, interpolate, drop)
MISSING_DATA_METHOD=ffill

# Máximo de dias consecutivos para forward-fill
MAX_FFILL_DAYS=3

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE OUTPUT
# -----------------------------------------------------------------------------

# Formato de figuras (png, pdf, svg)
FIGURE_FORMAT=png

# DPI para figuras
FIGURE_DPI=150

# Tamanho padrão de figuras (largura,altura em polegadas)
FIGURE_SIZE=14,10

# Salvar dados intermediários (True/False)
SAVE_INTERMEDIATE=True

# Verbose mode (True/False) - mostrar logs detalhados
VERBOSE=False

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE REDE
# -----------------------------------------------------------------------------

# Timeout para download de dados (segundos)
DOWNLOAD_TIMEOUT=30

# Número de tentativas em caso de falha
MAX_RETRIES=3

# Delay entre tentativas (segundos)
RETRY_DELAY=5

# User agent para requisições (caso necessário)
USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36

# -----------------------------------------------------------------------------
# CAMINHOS CUSTOMIZADOS (Opcional)
# -----------------------------------------------------------------------------

# Se quiser usar estrutura de pastas diferente
# DATA_RAW_PATH=data/raw
# DATA_PROCESSED_PATH=data/processed
# FIGURES_PATH=figures
# OUTPUT_PATH=output

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES AVANÇADAS
# -----------------------------------------------------------------------------

# Usar cache para downloads (True/False)
USE_CACHE=True

# Número de processos paralelos (0 = auto, 1 = serial)
N_JOBS=0

# Seed para reprodutibilidade
RANDOM_SEED=42

# Nível de logging (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# NOTIFICAÇÕES (Opcional - para pipelines automatizados)
# -----------------------------------------------------------------------------

# Email para notificações de conclusão/erro (requer configuração SMTP)
NOTIFICATION_EMAIL=

# Webhook para Slack/Discord (opcional)
WEBHOOK_URL=

# -----------------------------------------------------------------------------
# MODO DE DESENVOLVIMENTO
# -----------------------------------------------------------------------------

# Ativar modo debug (mais logs, validações extras)
DEBUG_MODE=False

# Salvar plots intermediários durante processamento
SAVE_DEBUG_PLOTS=False

# Limitar número de observações para testes rápidos (0 = sem limite)
MAX_OBSERVATIONS_DEBUG=0

# -----------------------------------------------------------------------------
# NOTAS IMPORTANTES
# -----------------------------------------------------------------------------
#
# 1. Variáveis vazias usarão valores padrão definidos nos scripts
# 2. True/False são case-sensitive (use maiúsculas)
# 3. Datas devem estar no formato ISO (YYYY-MM-DD)
# 4. Para desabilitar uso do .env, passe --ignore-env ao executar scripts
# 5. Variáveis de ambiente têm precedência sobre .env
#
# =============================================================================

# EXEMPLO DE USO EM PYTHON:
# -------------------------
# from dotenv import load_dotenv
# import os
# 
# load_dotenv()
# 
# fred_key = os.getenv('FRED_API_KEY')
# start_date = os.getenv('START_DATE', '2020-01-01')  # valor padrão se não definido
# var_lag = int(os.getenv('VAR_MAX_LAG', 6))
#
# =============================================================================

# EXEMPLO DE USO EM BASH:
# -----------------------
# source .env
# python scripts/00_download_data.py --start-date $START_DATE --end-date $END_DATE
#
# =============================================================================